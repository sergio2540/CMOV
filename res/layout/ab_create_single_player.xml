<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="my.game.achmed.Activities.ABCreateSinglePlayer"
    tools:ignore="MergeRootFrame" 
    android:orientation="vertical"
    android:weightSum="6" >

    <ListView
        android:id="@+id/playerslist"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_above="@+id/createPlayerContainer"
        android:textSize="20sp" />

    <LinearLayout
        android:id="@+id/createPlayerContainer"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_alignParentBottom="true"
        android:weightSum="6" >

        <EditText
            android:id="@+id/playerName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="4"
            android:ems="10"
            android:inputType="text" />

        <Button
            android:id="@+id/createPlayerName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:text="@string/buttonCreatePlayer" />

    </LinearLayout>

</RelativeLayout>